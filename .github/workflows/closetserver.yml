name: remote ssh command

on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      
      with:
        host: 20.51.190.19
        username: azureuser 
        secret: secret.azurekey
        port: 22
